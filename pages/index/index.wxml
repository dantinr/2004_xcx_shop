<!--index.wxml-->
<view>
  <button type="primary" bindtap="btnLogin">登录</button>

  <view > 商品列表 </view>
  <view wx:for="{{list}}" wx:key="list" class="goods-item">
    {{item.goods_id}} - {{item.goods_name}} -  {{item.shop_price}}
   </view>
</view>
